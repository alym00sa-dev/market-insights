# Data Directory

This directory contains generated data files (gitignored):

- `events.db` - SQLite database with Market Signal Events
- `vector_store/` - ChromaDB vector embeddings for semantic search

These files are created automatically when you run:
```bash
python src/main.py init
```
